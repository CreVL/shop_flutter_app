import 'package:flutter/material.dart';

class PoductDetailsScreen extends StatelessWidget {
  const PoductDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
